# Gather Windows

macOS app that shows numbered overlays on each screen, letting you pick a target display to gather all windows to. Also supports CLI mode when invoked with arguments.

## Conventions

- Always use TDD: write failing tests in `gather-windowsTests` before writing implementation code.
- Always run `make` when completing a task that involves modifying code or assets.
