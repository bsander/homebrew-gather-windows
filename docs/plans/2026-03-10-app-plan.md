# Gather Windows — Implementation Plan

Each step is independently verifiable. TDD: write failing test first, then implement.

---

## Phase 1: Refactor core to support arbitrary target display

### Step 1.1: Add ScreenNumbering logic
- **Test**: `ScreenNumberingTests` — main display gets 1, others sorted left-to-right get 2, 3...
- **Implement**: `ScreenNumbering.swift` — pure function taking screen frames/isMain, returns numbered list
- **Verify**: `swift test`

### Step 1.2: Modify WindowManager to accept arbitrary target display
- **Test**: Update `WindowManagerTests` — pass a specific target `DisplayInfo` instead of always using built-in
- **Implement**: Change `WindowManager.moveWindows()` to take a `targetDisplay: DisplayInfo` parameter
- **Verify**: All existing tests still pass with updated signatures

### Step 1.3: Update DisplayManager for target selection
- **Test**: `DisplayManagerTests` — add `displayForNumber(_:)` that returns the correct display
- **Implement**: Method that maps screen number to DisplayInfo using ScreenNumbering
- **Verify**: `swift test`

---

## Phase 2: Convert to macOS app bundle

### Step 2.1: Update Package.swift for app target
- Change executable type, add SwiftUI framework dependency
- Update Makefile for `.app` bundle building
- **Verify**: `make build` produces a .app bundle

### Step 2.2: Create App.swift entry point
- **Implement**: `@main` App struct that checks `CommandLine.arguments`
  - Args present → CLI path (Phase 3)
  - No args → overlay path (Phase 3)
- **Verify**: App launches and quits cleanly

### Step 2.3: Add Info.plist with LSUIElement
- `LSUIElement = true` (no dock icon)
- Bundle identifier, name, etc.
- **Verify**: App launches with no dock icon

---

## Phase 3: Overlay mode

### Step 3.1: Create OverlayView (SwiftUI)
- **Implement**: SwiftUI view — large white number centered on dark semi-transparent background
- **Verify**: Preview in Xcode / visual test

### Step 3.2: Create OverlayWindow
- **Implement**: NSWindow subclass — borderless, screenSaver level, positioned per screen
- Set collection behavior for all spaces
- **Verify**: Launch app → overlays appear on all screens with correct numbers

### Step 3.3: Add KeyboardHandler
- **Test**: Unit test that handler calls correct callback for number keys and Escape
- **Implement**: `NSEvent.addLocalMonitorForEvents` — number keys trigger gather, Escape triggers quit
- **Verify**: Press number → gathers → quits. Press Escape → quits.

### Step 3.4: Wire overlay mode end-to-end
- **Implement**: Connect KeyboardHandler → close overlays → WindowManager.moveWindows → NSApp.terminate
- **Verify**: Full flow works: launch → overlays → press number → windows move → app quits

### Step 3.5: Single-screen toast
- **Implement**: If only one screen, post UNUserNotificationCenter notification, then quit
- **Verify**: Disconnect external display → launch app → toast appears → app quits

---

## Phase 4: CLI mode

### Step 4.1: Parse CLI arguments
- **Implement**: In App.swift, detect args. Parse positional: number or "main"
- **Verify**: `./Gather\ Windows 2` gathers to screen 2, `./Gather\ Windows main` gathers to main

### Step 4.2: CLI error handling
- **Implement**: Invalid arg → print usage to stderr → exit(1)
- **Verify**: `./Gather\ Windows abc` prints usage and exits 1

### Step 4.3: Symlink support in Makefile
- **Implement**: `make dev` creates symlink from app binary to `/usr/local/bin/gather-windows`
- **Verify**: `gather-windows main` works from terminal

---

## Phase 5: Polish

### Step 5.1: Existing flags (--verbose, --include-fullscreen, --hide-during-move)
- Decide: keep, drop, or adapt for both modes
- If keeping for CLI mode, parse them manually (no swift-argument-parser)

### Step 5.2: Update README and help text

### Step 5.3: Run `make` — full build + test pass
